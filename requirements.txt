requests>=2.31.0,<3.0.0
python-dotenv>=1.0.0

# Only needed for the old always-on gateway bot (bot.py). The scheduled
# check (check_update.py) does not use these.
discord.py>=2.3.0,<3.0.0
aiohttp>=3.9.0,<4.0.0
